

v1 : ping cmd implmented.  ping from board to target device as well as ping to the board is working. default ip is 192.168.2.207.

v2 : dhcp cmd , dhcp will request dhcp ip fro ma server. use status cmd to know the new ip address.

v3 : added the simple http server.  both the http server/ and the icmp ( ping service) will work parallel
v4 : added help command, monitor off/on for traffic monitor, detiled status info
v5 : added autodetection of link down and reconnect, also added iperf server, iperf-test.py is used for testing


